export enum EMAIL_VERIFICATION_STATUS_ENUM {
    INVALID = "INVALID",
    PENDING = "PENDING",
    VALID = "VALID"
};

export enum GENDER_ENUM {
    MALE = "MALE",
    FEMALE = "FEMALE",
    OTHER = "OTHER",
    PREFER_NOT_SAY = "PREFER_NOT_SAY"
};

export enum AUTH_PROVIDER_ENUM {
    GOOGLE = "google",
    CREDENTIALS = "credentials"
};

export enum APPS_ENUM {
    CORE = "core",
    INTERVIEWLY = "interviewly",
    HELLO_HR = "hello_hr",
    PLUS_PLATFORM = "plus_platform"
};

export enum CLASSCONTENT_STATUS_ENUM {
    PUBLISHED = "published",
    UPCOMING = "upcoming"
};

export enum CONTENT_TYPES_ENUM {
    DATASET = "dataset",
    WORKSHOP = "workshop",
    LESSON = "lesson",
    PROTOCOL = "protocol"
};